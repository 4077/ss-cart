<div class="{__NODE_ID__} {CLASS}" instance="{__INSTANCE__}">

    <div class="header">{HEADER}</div>

    <div class="table">
        {TABLE}
    </div>

    <div class="form">
        {FORM}
    </div>

</div>
